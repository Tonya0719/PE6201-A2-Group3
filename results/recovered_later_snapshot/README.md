# Recovered Later-Snapshot Results

These files preserve previously generated team evidence from a later development snapshot.

They are **historical/provisional evidence**, not automatically the final evidence for the current merged code. The current baseline contains stronger runtime controls and a corrected D2(b) one-tool experiment. If the final eval set or code changes, rerun the corresponding experiment and write the new official outputs to the normal `results/` locations.

Do not cite the recovered D2(b) result as the final controlled comparison: that older experiment changed more than one tool descriptor and therefore does not isolate one interface rewrite.
